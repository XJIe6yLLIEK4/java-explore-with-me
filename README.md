# ExploreWithMe

**ExploreWithMe** — это микросервисное приложение для создания и управления событиями, позволяющее пользователям находить компанию для участия в интересных мероприятиях.

## 📖 Описание проекта

Приложение представляет собой афишу, где пользователи могут предлагать какое-либо событие от выставки до похода в кино и находить компанию для участия в нём.

### Основные возможности:
- 🎫 **Управление событиями** — создание, редактирование и публикация событий
- 👥 **Система пользователей** — регистрация и управление профилями
- 💬 **Комментарии** — обсуждение событий с модерацией
- 📝 **Заявки на участие** — подача и обработка запросов на участие в событиях
- 📊 **Подборки событий** — кураторские коллекции мероприятий
- 📈 **Статистика** — аналитика просмотров и активности
- 🏷️ **Категории** — классификация событий по типам

## 🏗️ Архитектура

Проект построен на микросервисной архитектуре и состоит из двух основных сервисов:

### 1. **EWM Main Service** (порт 8080)
Основной сервис приложения, предоставляющий:
- **Public API** — публичный доступ к событиям и категориям
- **Private API** — функции для авторизованных пользователей
- **Admin API** — административные функции

### 2. **Stats Service** (порт 9090)
Сервис статистики для сбора и анализа данных о просмотрах

### База данных
- **PostgreSQL** — две отдельные базы данных для каждого сервиса
- **JPA/Hibernate** — для работы с данными

## 🛠️ Технологии

- **Java 17**
- **Spring Boot 3.3.2**
- **Spring Data JPA**
- **PostgreSQL 16**
- **Maven** — система сборки
- **Docker & Docker Compose** — контейнеризация
- **Lombok** — уменьшение boilerplate кода
- **OpenAPI 3** — документация API

### Инструменты качества кода:
- **Checkstyle** — проверка стиля кода
- **SpotBugs** — статический анализ
- **JaCoCo** — покрытие тестами

## 🚀 Запуск приложения

### Предварительные требования
- Docker и Docker Compose
- Java 17 (для локальной разработки)
- Maven 3.6+ (для локальной разработки)

### Быстрый старт с Docker

1. Клонировать репозиторий:
```bash
git clone https://github.com/XJIe6yLLIEK4/java-explore-with-me.git
cd java-explore-with-me
```

2. Запустить приложение:
```bash
docker-compose up -d
```

3. Проверить работу сервисов:
- **Main Service**: http://localhost:8080
- **Stats Service**: http://localhost:9090

### Локальная разработка

1. Установить зависимости:
```bash
mvn clean install
```

2. Запустить базы данных:
```bash
docker-compose up stats-db ewm-db -d
```

3. Запустить сервисы:
```bash
# Stats Service
cd stats/stats-server
mvn spring-boot:run

# Main Service (в другом терминале)
cd ewm-main-service
mvn spring-boot:run
```

## 📚 API Документация

### Main Service (EWM)
- **OpenAPI**: доступна в файле `ewm-main-service-spec.json`
- **Swagger UI**: http://localhost:8080/swagger-ui.html (при запуске с соответствующими настройками)

### Stats Service
- **OpenAPI**: доступна в файле `ewm-stats-service-spec.json`

### Основные эндпоинты Main Service:

#### Public API
- `GET /events` — получение событий
- `GET /events/{id}` — получение события по ID
- `GET /categories` — получение категорий
- `GET /compilations` — получение подборок

#### Private API (требует авторизации)
- `POST /users/{userId}/events` — создание события
- `PATCH /users/{userId}/events/{eventId}` — редактирование события
- `POST /users/{userId}/requests` — подача заявки на участие

#### Admin API
- `POST /admin/users` — создание пользователя
- `POST /admin/categories` — создание категории
- `PATCH /admin/events/{eventId}` — модерация события

## 🧪 Тестирование

Коллекция Postman для тестирования API находится в папке `/postman/`:
- `feature.json` — полный набор тестов с проверками комментариев

### Запуск тестов Maven:
```bash
mvn test
```

### Проверка качества кода:
```bash
mvn clean compile -P check
```

### Анализ покрытия тестами:
```bash
mvn clean test -P coverage
```

## 🔧 Конфигурация

### Переменные окружения

#### Main Service:
- `SPRING_DATASOURCE_URL` — URL базы данных
- `SPRING_DATASOURCE_USERNAME` — пользователь БД
- `SPRING_DATASOURCE_PASSWORD` — пароль БД
- `STATS_BASE_URL` — URL сервиса статистики

#### Stats Service:
- `SPRING_DATASOURCE_URL` — URL базы данных статистики
- `SPRING_DATASOURCE_USERNAME` — пользователь БД
- `SPRING_DATASOURCE_PASSWORD` — пароль БД

### Профили
- `default` — основной профиль для продакшена
- `test` — профиль для тестирования

## 📊 Структура проекта

```
java-explore-with-me/
├── ewm-main-service/          # Основной сервис
│   ├── src/main/java/
│   │   └── ru/practicum/
│   │       ├── category/      # Управление категориями
│   │       ├── comment/       # Система комментариев
│   │       ├── compilation/   # Подборки событий
│   │       ├── event/         # Управление событиями
│   │       ├── request/       # Заявки на участие
│   │       └── user/          # Управление пользователями
│   └── Dockerfile
├── stats/                     # Сервис статистики
│   ├── stats-client/          # Клиент для обращения к статистике
│   ├── stats-dto/             # Общие DTO
│   └── stats-server/          # Сервер статистики
├── postman/                   # Postman коллекции
├── docker-compose.yml         # Конфигурация Docker
└── pom.xml                    # Maven конфигурация
```

## 🔄 Workflow разработки

1. **Создание ветки** от `main` для новой функциональности
2. **Разработка** с использованием TDD подхода
3. **Проверка качества**:
   ```bash
   mvn clean compile -P check    # Checkstyle + SpotBugs
   mvn test -P coverage          # Тесты + покрытие
   ```
4. **Создание Pull Request** с описанием изменений
5. **Code Review** и тестирование
6. **Merge** в основную ветку

## 📝 Особенности реализации

### Система комментариев
- Модерация комментариев администраторами
- Статусы: `PENDING`, `APPROVED`, `REJECTED`
- Возможность комментирования только опубликованных событий

### Управление событиями
- Жизненный цикл: `PENDING` → `PUBLISHED` / `CANCELED`
- Лимиты участников и подтверждение заявок
- Автоматический подсчет статистики просмотров

### Безопасность
- Валидация входных данных
- Проверка прав доступа на уровне контроллеров
- Обработка исключений с информативными сообщениями

## 🤝 Контрибьюция

1. Fork репозитория
2. Создайте ветку для новой функциональности
3. Внесите изменения с тестами
4. Убедитесь, что все проверки проходят
5. Создайте Pull Request

## 📄 Лицензия

Проект создан в учебных целях.
